# My 3D - Проект демонстрации 3D моделей

Веб-приложение для демонстрации создаваемых 3D моделей с их описанием и техническими характеристиками.

## Технологии

- **Framework:** Nuxt 4
- **Language:** TypeScript
- **UI:** Vue 3, Nuxt UI
- **3D:** Three.js
- **Styling:** SCSS, CSS Modules, Tailwind CSS
- **Internationalization:** @nuxtjs/i18n
- **Package Manager:** Yarn

## Требования

- Node.js: `^20.19.0 || >=22.14.0`
- Yarn: `1.22.22+`

## Установка

Установите зависимости:

```bash
yarn install
```

## Запуск

### Разработка

Запуск dev-сервера на `http://localhost:3000`:

```bash
yarn dev
```

### Сборка

```bash
yarn build
```

### Предпросмотр production сборки

```bash
yarn preview
```

## Структура проекта

```
app/
├── assets/          # Статические ресурсы (стили, иконки, изображения)
├── components/      # Vue компоненты
│   ├── layout/      # Компоненты макета (Header)
│   ├── main/        # Компоненты главной страницы
│   └── portfolio/   # Компоненты портфолио
├── composables/     # Композаблы (автоимпорт)
├── data/            # Данные моделей
├── layouts/         # Макеты страниц
├── pages/           # Страницы (автоматический роутинг)
└── types/           # TypeScript типы

i18n/                # Файлы локализации
public/              # Публичные файлы (модели, изображения)
```

## Основные страницы

- `/` - Главная страница с интерактивной 3D моделью
- `/portfolio` - Портфолио с карточками моделей
- `/admin` - Админ-панель для управления изображениями (требует пароль)

## Добавление моделей

Добавьте данные модели в `app/data/models.ts` и соответствующие файлы (изображения, 3D модели) в `public/models/`.

## Админ-панель

Проект включает админ-панель на странице `/admin` для управления изображениями.

### Особенности

- ✅ **Работает на статическом хостинге** - не требует Node.js сервера
- ✅ **Хранение в браузере** - изображения сохраняются в IndexedDB
- ✅ **Простая аутентификация** - вход по паролю

### Настройка

1. Установите пароль в `.env`:
   ```env
   NUXT_PUBLIC_ADMIN_PASSWORD=ваш_пароль
   ```

2. После сборки админка будет доступна на `/admin`

⚠️ **Важно:** Пароль будет виден в коде клиента. Используйте сложный пароль для production.

## Деплой на Timeweb

Проект настроен для автоматической сборки и деплоя на хостинг Timeweb через GitHub Actions.

📖 **Подробная инструкция по настройке:** см. [DEPLOY_SETUP.md](./DEPLOY_SETUP.md)

### Краткая инструкция

1. **Создайте Environment в GitHub** (рекомендуется):
   - Settings → Environments → New environment → `production`
   - Добавьте секреты в Environment secrets

2. **Или используйте Repository secrets** (проще):
   - Settings → Secrets and variables → Actions → New repository secret

3. **Необходимые секреты**:
   - `TIMEWEB_HOST` - адрес FTP/SFTP сервера
   - `TIMEWEB_USERNAME` - имя пользователя FTP
   - `TIMEWEB_PASSWORD` - пароль FTP
   - `TIMEWEB_REMOTE_PATH` - путь на сервере (например, `/public_html`)

4. **Автоматический деплой**:
   - При каждом push в ветку `main` или `master` будет автоматически запускаться сборка и деплой
   - Также можно запустить деплой вручную через вкладку "Actions" в GitHub

### CI/CD через GitHub Actions (использование)

- Автодеплой запускается при push в `main`/`master`.
- Сборка выполняется командой `yarn generate` (SSG). Артефакты попадают в `.output/public/`.
- Если Nuxt генерирует только `200.html`, workflow автоматически создаёт `index.html` из `200.html`.
- Файл `.ftp-deploy-sync-state.json` исключён из выгрузки и удаляется на сервере в debug-режиме.
- Включена `concurrency`: новый деплой отменит предыдущий незавершённый для той же ветки.

#### Ручной запуск с параметрами
1. Зайдите в вкладку **Actions** → **Build and Deploy to Timeweb** → **Run workflow**.
2. Параметры:
   - `full_clean` — `false` по умолчанию. Если `true`, запускается «чистый» деплой (удаление содержимого `public_html` перед загрузкой).
   - `debug` — `false` по умолчанию. Если `true`, включаются подробные логи и диагностические листинги директорий.

Рекомендация: используйте `full_clean=true` только при редких случаях (например, после смены структуры файлов).

### Ручной деплой

Если нужно задеплоить вручную:

```bash
# Сборка проекта
yarn build

# Файлы для деплоя находятся в .output/public/
# Загрузите содержимое этой папки на сервер через FTP/SFTP клиент
```

### Структура деплоя

После сборки все файлы для деплоя находятся в `.output/public/`. Эта директория содержит:
- `index.html` - главный HTML файл
- Статические ресурсы (CSS, JS, изображения)
- Файлы из папки `public/` (модели, изображения и т.д.)

### Диагностика

- Если сайт отдаёт `403 Forbidden`, проверьте, что на сервер выгружен `index.html` в корень `public_html`.
- Если изменения не видны, запустите workflow с `debug=true` и посмотрите листинги `.output/public/` в логах.
- При аномалиях синхронизации запустите ручной деплой с `full_clean=true`.

